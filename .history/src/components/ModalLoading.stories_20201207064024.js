import React from 'react';
import {storiesOf} from '@storybook/react-native';

import ModalLoading from './TestComponent';

storiesOf('Common Components', module).add('ModalLoading', () => (
  <ModalLoading />
));
