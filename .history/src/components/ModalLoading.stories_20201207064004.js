import React from 'react';
import {storiesOf} from '@storybook/react-native';

import ModalLoading from './TestComponent';

storiesOf('Test Component', module).add('example', () => <ModalLoading />);
