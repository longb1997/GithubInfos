import React from 'react';
import { storiesOf } from '@storybook/react-native';

import LoadMoreButton from '../LoadMoreButton';

storiesOf('Common Components', module).add('LoadMoreButton', () => <LoadMoreButton />);
