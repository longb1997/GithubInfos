import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  totalRepo: {
    alignItems: 'center',
    paddingTop: 12,
  },
  totalRepoText: {
    fontSize: 18,
    fontWeight: '400',
    textAlign: 'center',
  },
});

export default styles;
